<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>SiteLayout - @ViewBag.Title</title>
</head>
<body>
    <div>
        @RenderBody();
    </div>
    @if (IsSectionDefined("scripts")) {
       @RenderSection("scripts", required: false);
    }
</body>
</html>
